# CHANGELOG

Формируется только из подтверждённых фактов Git (SHA, дата, сообщение
коммита). Ничего не додумывается. `scripts/checkpoint-freex.ps1` не
переписывает историю автоматически — он может только предложить текст новой
записи, добавление делает человек/агент вручную.

Всего коммитов в истории на 2026-07-13: 128 (`git log --oneline | wc -l`).
Ниже — то, что подтверждено привязкой к версии в коде (`js/config.js`,
`APP_VERSION`) и тегу `v8.0-before-rebuild`. Более ранняя история версий не
реконструирована по номерам: до объединения в 8.1 в разных местах кода
одновременно встречались противоречащие друг другу номера версий (подробности
— `agents/gpt.md`, раздел «Аудит архитектуры»). Полный список коммитов —
`git log`.

## [FreeX 8.1] — 2026-07-13

Подтверждено: `js/config.js` → `APP_VERSION = 'FreeX 8.1'`.

- `8cce59a` (2026-07-13 05:50) — docs: update FreeX 8.1 project memory
  Синхронизация документации (`agents/gpt.md`, `start.md`, `README.md`) с
  фактическим состоянием кода после миграции на 8.1.
- `c30b078` (2026-07-13 05:46) — fix: protect notes and drafts
  Автосохранение черновика заметки, восстановление при повторном открытии,
  предупреждение перед потерей несохранённого, toast вместо части `alert()`.
- `be04faf` (2026-07-13 05:45) — fix: stabilize chats and conference
  Таймаут 55с на запрос к модели (`js/api.js`), чтобы режим «Конференция» не
  мог зависнуть навсегда на одном провайдере.
- `ab09862` (2026-07-13 05:45) — refactor: migrate FreeX to modular runtime
  Инлайн-скрипт в `index.html` удалён (−464 строки), единственная точка
  входа — `js/app.js` (+321 строки). Исправлены баги, найденные при сверке
  модулей с рабочим кодом: Dexie-импорт в `js/db.js`, дубль-импорт в
  `js/chat.js`, битый `onclick` в `js/sidebar.js`.
- `c3f70ea` (2026-07-12 23:42) — feat: add native DeepSeek provider
  Прямая интеграция `api.deepseek.com`; приоритет ключей — сначала личный
  `apiKey` пользователя, иначе `DEEPSEEK_API_KEY` из окружения Vercel;
  `reasoning_content` возвращается отдельным полем, не смешивается с `content`.

## [v8.0-before-rebuild] (тег) — 2026-07-12

- `5f2deed` (2026-07-12 20:53) — FreeX: создание agents/abacus.md
  Тег и резервная ветка `backup/before-freex-8.1` созданы от этого коммита
  как точка отката перед перестройкой `main` в версию 8.1.

## Более ранняя история

Не сведена к списку версий в этом файле, чтобы не приписывать задним числом
номера версий коммитам, где в самом коде они были противоречивы (одновременно
`v6.2`/`8.0`/`5.0` в разных файлах — см. `agents/gpt.md`). Хронологический
нарратив по датам — в `start.md`, раздел «9. История версий». Точные коммиты —
`git log --oneline`.
